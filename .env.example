# Server Configuration
HOST=127.0.0.1
PORT=8000
DEBUG=True

# Indian Market Data Feed (MOCK | LIVE)
MARKET_DATA_PROVIDER=MOCK
MOCK_UPDATE_INTERVAL_SEC=1.0

# Optional: Pydantic AI / LLM Configuration
# The system includes a deterministic fallback engine and runs 100% offline if no keys are provided.
# LLM_PROVIDER=gemini # options: gemini | openai | anthropic | groq
# GEMINI_API_KEY=
